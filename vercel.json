{
  "functions": {
    "api/*.py": {
      "runtime": "python3.12"
    }
  },
  "builds": [
    { "src": "api/webhook.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/webhook.py" }
  ]
}
